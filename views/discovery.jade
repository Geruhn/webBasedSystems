extends layout

append head
	script(src="/js/discovery.js")
	script(src="/js/locationTable.js")

block content
	//- var tableLocations = Array();
	input#vicinityRange(type="range", min=0, step="0.5", max="100", value=5)
	label#vicinityRangeLabel(for="vicinityRange")
	block locations
		- var tableLocations = locations
		include ./includes/locationsTable.jade
